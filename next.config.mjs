/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains:["grandeursmart.com"]
      },
    output: "export"
};

export default nextConfig;
